//
// Created by Marc Haubenstock on 2019-07-22.
//

#include "frame.h"

frame::Loaded_Frames frame::load_frames(const vector<string> &reference_image_paths,
                                 const vector<string> &reference_depth_paths, const vector<string> &target_image_paths,
                                 const vector<string> &target_depth_paths, image_data depth_factor, float direction,
                                 ImageFilter filter_x, ImageFilter filter_y, bool standardize, uint8_t pyramid_levels,
                                 float sigma) {
    Loaded_Frames loaded_frames;

    //TODO:

    return loaded_frames;
}